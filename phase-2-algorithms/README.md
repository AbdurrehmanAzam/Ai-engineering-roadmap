# Phase 2: Algorithm Engineering

## Goals
- Solve ~50 LeetCode problems with clean, well-documented solutions.
- Implement core data structures (arrays, stacks, queues, hash maps, trees).
- Build intuition for time/space complexity.

## Key folders (planned)
- `leetcode/` — solution files and notes.
- `patterns/` — reusable algorithm templates.

## How to run
This phase is not started yet. When code is added, run scripts with:
```bash
python <solution_file>.py
```
